my-eshell:
  - enable external command arg completion
  - syntax coloring in eshell prompt for git status and others

my-easypg:
  - make gpg passphrase insertion wait if it is not introduces
  (currently it lets the load process continue and if you enter the pass
  later you get: GETPIN error (error "Selecting deleted buffer"))

notmuch:
  - use locally compiled notmuch exe (not the binary shipped by distros)
  - apply patch  & use encrypted notmuch config from local exe
  - use saved searches instead of tags
  - call redraw-redisplay after creating a new window on notmuch-hello
